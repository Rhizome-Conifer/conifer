import { actionKey } from 'config';

const component = 'Settings';

export const LOAD_SETTINGS = `${actionKey + component}/LOAD_SETTINGS`;
export const LOAD_SETTINGS_SUCCESS = `${actionKey + component}/LOAD_SETTINGS_SUCCESS`;
export const UPDATE_SETTINGS = `${actionKey + component}/UPDATE_SETTINGS`;
export const UPDATE_SETTINGS_SUCCESS = `${actionKey + component}/UPDATE_SETTINGS_SUCCESS`;
