import { actionKey } from 'config';

const component = 'Dashboard';

export const LOAD_DASHBOARD = `${actionKey + component}/LOAD_DASHBOARD`;
export const LOAD_DASHBOARD_SUCCESS = `${actionKey + component}/LOAD_DASHBOARD_SUCCESS`;

