import { actionKey } from 'config';

const component = 'UserDetail';

export const LOAD_USER = `${actionKey + component}/LOAD_USER`;
export const LOAD_USER_SUCCESS = `${actionKey + component}/LOAD_USER_SUCCESS`;
export const UPDATE_USER = `${actionKey + component}/UPDATE_USER`;
export const UPDATE_USER_SUCCESS = `${actionKey + component}/UPDATE_USER_SUCCESS`;
