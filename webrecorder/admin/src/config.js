/* global config variables */
/* eslint-disable no-unused-vars */

// can be FQDN to host externally e.g. http://custom-webrecorder.io/api
// if relative, the current location will be used.
export const endpoint = '/api/v1';
export const actionKey = 'wr/';
